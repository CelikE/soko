---
bump: minor
---

soko ui: mark repos with space (* for all) and batch-pull them with one confirmed P, undo the last pull with u, fetch a single repo with R, copy the repo path with y; pull errors now include the git failure detail, --fetch gets a 30s floor, and quitting mid-pull asks twice
