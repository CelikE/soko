---
bump: minor
---

soko ui: grouped view redesigned — accent-colored `▾ tag (n)` headers carry each group's dirty/crit totals, rows are indented under their header, and the column header shifts to keep columns aligned
