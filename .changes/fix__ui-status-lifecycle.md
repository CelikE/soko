---
bump: patch
---

soko ui: transient status/error lines now expire instead of sticking forever, the fetching indicator survives concurrent cheap refreshes, refresh ticks pause while a fetch is in flight, and the status line shows when the last fetch happened
